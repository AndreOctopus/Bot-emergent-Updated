import React from 'react';
import TradingDashboard from './TradingDashboard';  // Додай імпорт дашборду

export default function LoginPage() {
  return <TradingDashboard />;  // Замість логіну — відразу дашборд
}
